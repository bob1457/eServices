
@{
    ViewData["Title"] = "Secure";
    //Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Secure</h1>

<di>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dt>@claim.Value</dt>
    }
</di>

